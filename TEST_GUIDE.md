# 에러/토스트 메시지 테스트 가이드

Android(Kotlin)와 Flutter 앱에서 **동일한** 에러·성공 메시지가 표시되는지 확인하는 방법입니다.

---

## 1. 표시 메시지 목록 (Kotlin = Flutter 동일)

| 표시 메시지 | 발생 상황 |
|------------|-----------|
| **가입 안된 계정** | 가입하지 않은 이메일로 로그인 시도 |
| **잘못된 계정입니다** | 이메일 로그인 시 비밀번호 오류 |
| **네트워크 연결 상태가 좋지 않습니다** | 인터넷 없음 상태에서 로그인/저장 시도 |
| **서버 지연** | 요청 타임아웃 등 |
| **토큰 문제** | Firebase Auth 관련 오류 |
| **DB 저장 실패** | Firestore 작업 실패 |
| **unlink 오류** | 카카오/네이버 회원탈퇴(unlink) 실패 시 |
| **로그인 실패** | 소셜(Google/Kakao/Naver) 로그인 중 예외(원인 불명 포함) |
| **기타** | 위에 해당하지 않는 예외 |
| **회원탈퇴 실패** | 회원탈퇴 처리 실패 시 |

**성공/안내 메시지**

| 표시 메시지 | 발생 상황 |
|------------|-----------|
| **OOO님 환영합니다** | 로그인 성공 후 (OOO = 사용자 이름) |
| **로그아웃 되었습니다** | 로그아웃 성공 후 |
| **회원탈퇴 완료** | 회원탈퇴 성공 후 |

---

## 2. 에러 메시지별 테스트 방법


### 2.1 가입 안된 계정 ✅

**목표:** 토스트/스낵바에 `가입 안된 계정` 표시

**조건:** 해당 이메일로는 한 번도 회원가입한 적 없음.

**절차**

1. 앱 실행 → **기존 계정으로 로그인하기** 이동
2. **가입되지 않은 이메일** 입력 (예: `notregistered@test.com`)
3. 임의의 비밀번호 입력 후 로그인 시도
4. **확인:** `가입 안된 계정` 메시지 표시 (Android: Toast, Flutter: SnackBar)

**참고:** Firebase 이메일 열거 방지 설정에 따라 `invalid-credential`로 올 수 있어, 실제로는 `잘못된 계정입니다`가 나올 수 있음. 미가입 전용 테스트는 Firebase Console에서 해당 설정을 끄거나, 테스트용 계정을 미리 삭제한 뒤 진행.

---

### 2.2 잘못된 계정입니다 ✅

**목표:** 토스트/스낵바에 `잘못된 계정입니다` 표시

**조건:** 이미 가입된 이메일 계정이 있음.

**절차**

1. **기존 계정으로 로그인하기** 화면으로 이동
2. **가입된 이메일** 입력
3. **잘못된 비밀번호** 입력 후 로그인 시도
4. **확인:** `잘못된 계정입니다` 메시지 표시

---

### 2.3 네트워크 연결 상태가 좋지 않습니다 ✅

**목표:** 토스트/스낵바에 `네트워크 연결 상태가 좋지 않습니다` 표시

**방법 A: 실제 네트워크 차단**

1. 기기에서 **Wi‑Fi 끄기** + **모바일 데이터 끄기** (또는 비행기 모드)
2. 앱에서 다음 중 하나 수행:
   - **이메일 로그인** 시도, 또는 ✅
   - **소셜(카카오/네이버/구글) 로그인** 시도, 또는 ✅
   - 이미 로그인된 상태에서 **물 컵 추가/저장** 시도 ✅
3. **확인:** `네트워크 연결 상태가 좋지 않습니다` 메시지 표시

**방법 B: 에뮬레이터**

- Android: 에뮬레이터 설정에서 네트워크를 끄거나, `adb shell svc wifi disable` 등으로 차단
- iOS 시뮬레이터: 맥에서 네트워크 차단 후 동일 동작 수행

---

### 2.4 서버 지연

**목표:** 토스트/스낵바에 `서버 지연` 표시

**실기기/실네트워크:** 재현이 어렵습니다. 아래 중 하나 권장.

- **프록시/Charles 등:** Firebase/백엔드 요청에 긴 지연(예: 60초) 걸어서 타임아웃 유도
- **단위 테스트:** `AuthErrorMapper`에 `TimeoutException`(또는 타임아웃으로 변환되는 예외)을 넣었을 때 `"서버 지연"`이 반환되는지 검증

**예시 (Flutter 단위 테스트):**

```dart
test('map returns 서버 지연 for TimeoutException', () {
  expect(AuthErrorMapper.map(TimeoutException('')), '서버 지연');
});
```

---

### 2.5 토큰 문제 ✅

**목표:** 토스트/스낵바에 `토큰 문제` 표시

**가능한 상황:**

- Firebase Auth 세션은 있는데 Firestore 사용자 문서가 없음 (불일치)
- Firebase Auth 토큰 만료/무효
- Firebase 프로젝트 설정 오류로 인한 Auth 오류
- 소셜 로그인 후 Custom Token 생성 실패 등

---

#### 방법 A: Firestore 사용자 문서 삭제 (권장, 재현 쉬움)

**의도:** Auth에는 로그인된 상태인데 Firestore에 해당 uid 문서가 없을 때 `토큰 문제`를 보여주고, 메인 화면 로딩에 멈추지 않고 로그인 화면으로 돌아가게 함.

**절차:**

1. **테스트용 계정으로 로그인** (이메일 또는 소셜)
2. **앱을 완전히 종료하지 않은 채** Firebase Console로 이동  
   - [Firebase Console](https://console.firebase.google.com) → 프로젝트 선택
3. **Firestore Database** → `users` 컬렉션 → **방금 로그인한 사용자의 uid 문서** 선택 후 **삭제**
4. 앱으로 돌아와서 **다시 앱을 켜거나, 이미 켜져 있다면 화면을 다시 띄움**  
   - 또는 로그인된 상태에서 **앱 프로세스를 죽였다가 다시 실행**
5. **확인**
   - **`토큰 문제`** 스낵바/토스트가 한 번 표시되고
   - **로그인 화면**으로 이동한 상태여야 함 (메인 화면에서 로딩만 계속 도는 상태가 아니어야 함)

**기대 동작 요약:**

- **수정 전:** Firestore 문서만 삭제된 경우, Auth 세션만 남아 있어 `loadUser()`가 `null`을 반환하고, `state.user`가 설정되지 않아 메인으로 가지 않거나, 어떤 경로로는 메인에 진입했을 때 `user`/`todayLog`가 없어 **로딩만 계속 도는** 문제가 있을 수 있음.
- **수정 후:** Auth는 있는데 Firestore 사용자 문서가 없으면 `loadUserFromFireStore()`에서 예외를 던지고, ViewModel에서 `토큰 문제`를 띄운 뒤 **자동 로그아웃**하므로, 항상 **로그인 화면**에서 다시 로그인할 수 있음.

---

#### 방법 B: Firebase Auth 사용자 비활성화

1. Firebase Console → **Authentication** → **Users**에서 해당 사용자 선택
2. **비활성화(Disable)** 처리 후, 앱에서 해당 계정으로 로그인/갱신 시도
3. **확인:** `토큰 문제` 메시지 표시

---

#### 방법 C: 기타 (Custom Token 오류 등)

- Cloud Functions 등에서 의도적으로 잘못된 토큰을 반환하도록 한 뒤 소셜 로그인 시도
- **확인:** `토큰 문제` 메시지 표시

**주의:** 프로덕션 계정으로 하지 말고, 테스트 전용 프로젝트/계정으로 진행 권장.

---

### 2.6 DB 저장 실패 ✅

**목표:** 토스트/스낵바에 `DB 저장 실패` 표시

**방법:** Firestore 규칙을 일시적으로 막기

1. Firebase Console → Firestore → 규칙
2. `read, write: if false;` 처럼 **모두 거부**하는 규칙으로 저장
3. 앱에서 다음 중 하나 수행:
   - **회원가입(이메일)** 완료 시점( Firestore 사용자 문서 저장 시)
   - **로그인 후 물 기록 저장** 시도
4. **확인:** `DB 저장 실패` 메시지 표시
5. 테스트 후 **Firestore 규칙을 원래대로 복구**

---

### 2.7 unlink 오류

**목표:** 토스트/스낵바에 `unlink 오류` 표시

**상황:** 카카오/네이버 회원탈퇴(unlink) API 호출이 실패할 때.

**절차(개념):**

1. **카카오** 또는 **네이버**로 로그인된 상태에서 **회원탈퇴** 진행
2. unlink가 실패하도록 유도:
   - 카카오/네이버 개발자 콘솔에서 앱 설정·키 오류를 만들거나,
   - 네트워크 차단 후 탈퇴 시도(네트워크 메시지가 먼저 나올 수 있음)
3. **확인:** unlink 실패 시 `unlink 오류` 메시지 표시 (Repository/ViewModel에서 unlink 실패를 해당 예외로 매핑하는 경우)

**참고:** 실제 unlink 실패 시 앱이 어떤 예외를 던지는지에 따라, 메시지가 `회원탈퇴 실패`로만 나오도록 되어 있을 수 있음. Kotlin/Flutter 모두 회원탈퇴 실패 시 **회원탈퇴 실패**를 쓰고, unlink 전용 예외를 구분해 매핑한 경우에만 `unlink 오류`가 노출됨.

---

### 2.8 로그인 실패 (소셜 로그인) ✅

**목표:** 토스트/스낵바에 `로그인 실패` 표시

**절차**

1. **카카오 / 네이버 / 구글** 로그인 버튼 탭
2. 로그인 창에서 **취소**하거나, 계정 선택 후 예외가 나오도록 유도
3. **확인:** `로그인 실패` 메시지 표시 (OAuth 취소·기타 예외가 mapForOAuth에서 기타로 처리되는 경우)

---

### 2.9 기타

**목표:** 토스트/스낵바에 `기타` 표시

**방법:** AuthErrorMapper가 구체적으로 처리하지 않는 예외를 발생시킴.

- 예: ViewModel/Repository에서 `throw Exception('unknown')` 등으로 테스트용 예외를 던지고, 해당 경로에서 `AuthErrorMapper.map(e)`를 호출하도록 한 뒤 표시 확인
- **확인:** `기타` 메시지 표시

---

### 2.10 회원탈퇴 실패 ✅

**목표:** 토스트/스낵바에 `회원탈퇴 실패` 표시

**절차**

1. 로그인 후 **회원탈퇴** 진입
2. 이메일 계정이면 **비밀번호 잘못 입력** 후 탈퇴 시도
3. 또는 네트워크 차단 후 탈퇴 시도(네트워크 메시지가 먼저 나올 수 있음)
4. **확인:** `회원탈퇴 실패` 메시지 표시 (Kotlin: MainViewModel onFailure, Flutter: withdraw_dialog catch)

---

## 3. 성공/안내 메시지 테스트

### 3.1 OOO님 환영합니다 ✅

1. 로그인 화면에서 **이메일** 또는 **소셜** 로그인 성공
2. **확인:** `[사용자이름]님 환영합니다` 토스트/스낵바 표시 후 메인 화면으로 이동

### 3.2 로그아웃 되었습니다 ✅

1. 메인(또는 상단/설정)에서 **로그아웃** 실행
2. **확인:** `로그아웃 되었습니다` 메시지 표시 후 로그인 화면으로 이동

### 3.3 회원탈퇴 완료 ✅

1. **회원탈퇴** 플로우에서 본인 확인 후 탈퇴 완료
2. **확인:** `회원탈퇴 완료` 메시지 표시 후 로그인 화면으로 이동

---

## 4. 플랫폼별 참고

- **Android (Kotlin):** `UiEvent.ShowToast` → `Toast.makeText(..., Toast.LENGTH_SHORT).show()`
- **Flutter:** `AuthViewState.toastMessage` → `ScaffoldMessenger.showSnackBar(SnackBar(content: Text(msg)))`

같은 시나리오에서 **같은 문구**가 나오는지 위 표와 이 가이드 기준으로 확인하면 Kotlin과 Flutter 동작을 동일하게 검증할 수 있습니다.
