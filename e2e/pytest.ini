[pytest]
pythonpath = .
testpaths = tests
markers =
    streak_badge: Firestore 시드가 필요한 streak 뱃지 E2E
    notification: 알림 E2E (기존 계정 + 선택적 Firestore 시드)
    network_error: adb 로 네트워크를 끄는 오류 시나리오 (종료 시 복구)
