# WaterLog Appium + Python 자동화 테스트 케이스

현재 앱에 **실제로 구현된 UI/플로우** 기준으로, Appium + Python 모바일 UI 자동화에 적합한 기능을 정리한 문서입니다.

> 에러/토스트 메시지 수동 검증은 [`TEST_GUIDE.md`](./TEST_GUIDE.md)를 참고하세요.  
> 본 문서는 **E2E 시나리오 자동화** 관점의 테스트 케이스입니다.

---

## 1. 자동화 우선순위 요약

| 우선순위 | 영역 | 자동화 적합도 | 이유 |
|---------|------|--------------|------|
| P0 | 이메일 회원가입 / 로그인 | ★★★★★ | 앱 내부 플로우, 입력·검증·화면 전환이 명확 |
| P0 | 물 기록 (+/−) 시나리오 | ★★★★★ | 핵심 기능, 잔 수·ml·목표 문구로 assertion 가능 |
| P0 | 로그아웃 | ★★★★★ | 짧은 플로우, 결과 화면이 명확 | 
| P1 | 목표 달성 알림 (`goal_achieved`) | ★★★★☆ | 물 +로 조건 충족 → 알림 목록에서 검증 가능 |
| P1 | 환영 알림 (`welcome`) | ★★★★☆ | 신규 가입 직후 알림 목록에서 검증 가능 |
| P1 | 입력 유효성 검증 | ★★★★☆ | 네트워크 없이 재현 가능, 회귀 방지에 좋음 |
| P1 | 주간/월간 차트 토글 | ★★★★☆ | 탭 전환 + 텍스트 노출 검증이 쉬움 |
| P1 | 알림 목록 진입/읽음 | ★★★☆☆ | UI 진입은 쉬움, 알림 데이터는 사전 조건 필요 |
| P1 | 회원탈퇴 (이메일) | ★★★☆☆ | 자동화 가능하나 계정 소모·재인증 단계 있음 |
| P2 | 뱃지 획득 (조건 충족) | ★★☆☆☆ | Firestore 저장 로직은 있음, **뱃지 UI는 미연결** |
| P2 | 음료 잠금 해제 (뱃지/광고) | ★☆☆☆☆ | **현재 UI·로직 미구현** → 기능 완성 후 자동화 |
| P2 | 소셜 로그인 (카카오/네이버/구글) | ★★☆☆☆ | WebView·외부 앱 전환으로 flaky |
| P2 | 네트워크/에러 토스트 | ★★☆☆☆ | 네트워크 차단·백엔드 조작 필요 |
| — | 온보딩 / 설정 / 목표 편집 / AI 채팅 | 대상 아님 | 현재 UI 미구현 또는 플레이스홀더 |

---

## 2. 권장 자동화 범위 (먼저 잡을 것)

아래 시나리오는 **안정적이고 ROI가 높은** 자동화 후보입니다.

1. 이메일 회원가입 → 로그인 → 메인 진입
2. 물 잔 추가/감소 시나리오 → 잔 수·ml·목표 문구 변화
3. 목표 달성 시 알림 생성 → 알림 목록 확인
4. 주간 ↔ 월간 차트 전환
5. 로그아웃 → 로그인 화면 복귀

뱃지 획득·음료 잠금 해제는 **백엔드/기획 조건 기준으로 케이스를 정의해 두고**, UI가 연결되면 바로 자동화에 넣는 것을 권장합니다.  
소셜 로그인·시스템 푸시·광고 SDK는 초기 스위트에서 제외합니다.

---

## 3. 테스트 전제 / 공통 조건

| 항목 | 내용 |
|------|------|
| 대상 앱 | WaterLog (Flutter) |
| 도구 | Appium + Python |
| 권장 플랫폼 | Android Emulator 우선 (iOS는 별도 설정) |
| 테스트 계정 | 이메일 전용 테스트 계정 (생성/삭제 가능) |
| 일일 목표 | 기본 **8잔** (= 2L, 목표 변경 UI 없음) |
| 1잔 단위 | **250ml** |
| 뱃지(백엔드) | `day_2L`(하루 8잔), `week_7days`(연속 7일), `month_30days`(연속 30일), `king_6months`(연속 180일) |
| 알림(백엔드) | `welcome`(최초 가입/토큰), `goal_achieved`(당일 목표 최초 달성) |
| 음료 잠금/광고 | **현재 앱 UI 미구현** — 아래 TC는 기능 완성 후 자동화 대상 |
| Semantics / accessibility id | Flutter는 `Semantics(identifier: ...)` 또는 `ValueKey`를 심어두면 Appium Locating이 안정적 |

> Flutter + Appium 팁: 텍스트(`accessibility id` / xpath text)만으로도 가능하지만, 핵심 버튼(+/−, 로그인, 로그아웃)에는 `Semantics`/`Key`를 미리 넣어 두는 것이 좋습니다.

---

## 4. 테스트 케이스 상세

### TC-AUTH-01. 로그인 화면 기본 UI 노출

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 로그아웃 상태 |
| **절차** | 1. 앱 실행<br>2. 로그인 화면 진입 확인 |
| **기대결과** | `워터로그`, `매일 수분섭취를 기록하세요` 문구 노출<br>카카오 / 네이버 / 구글 로그인 버튼 노출<br>이메일 회원가입 / 기존 계정 로그인 진입 경로 노출 |
| **자동화 적합** | ★★★★★ |

---

### TC-AUTH-02. 이메일 회원가입 성공
### pytest tests/auth/test_email_sign_up.py::test_email_sign_up_success -v

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 미가입 이메일 사용 (예: `waterlog_e2e_{timestamp}@test.com`) |
| **절차** | 1. 로그인 화면 → 이메일 회원가입<br>2. 유효한 이메일/비밀번호/비밀번호 확인 입력<br>3. 서비스 이용약관·개인정보 처리방침 체크<br>4. 가입 완료 |
| **기대결과** | 회원가입 성공 후 로그인 흐름으로 복귀 또는 로그인 가능 상태<br>(구현: 가입 후 `/login` 복귀) |
| **자동화 적합** | ★★★★★ |
| **비고** | 비밀번호 규칙: 8자 이상 + 영문 + 숫자 + 특수문자 |

---

### TC-AUTH-03. 이메일 회원가입 — 유효성 검증 실패
### pytest tests/auth/test_email_sign_up_validation.py -v

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **전제** | 없음 |
| **절차** | 아래를 각각 검증<br>A. 잘못된 이메일 형식<br>B. 규칙 미충족 비밀번호<br>C. 비밀번호 ≠ 비밀번호 확인<br>D. 약관 미체크 상태에서 가입 시도 |
| **기대결과** | A. `이메일 주소형식에 맞게 입력해주세요`<br>B. `비밀번호는 8자 이상이며, 영문/숫자/특수문자를 모두 포함해야 합니다`<br>C. 비밀번호 확인 불일치 메시지<br>D. 가입 진행 불가(버튼 비활성 또는 안내) |
| **자동화 적합** | ★★★★☆ |
| **비고** | 네트워크 불필요 → 회귀 테스트에 매우 적합 |

---

### TC-AUTH-04. 이메일 로그인 성공
### pytest tests/auth/test_email_login.py -v

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 가입 완료된 테스트 계정 |
| **절차** | 1. `기존 계정으로 로그인` 진입<br>2. 이메일/비밀번호 입력<br>3. `로그인 하기` 탭 |
| **기대결과** | `{이름}님 환영합니다` SnackBar 표시<br>메인 화면(`WaterLog` AppBar) 진입 |
| **자동화 적합** | ★★★★★ |

---

### TC-AUTH-05. 이메일 로그인 실패 (잘못된 비밀번호)
### pytest tests/auth/test_email_login_failure.py -v

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **전제** | 가입된 계정 |
| **절차** | 올바른 이메일 + 잘못된 비밀번호로 로그인 |
| **기대결과** | `잘못된 계정입니다` SnackBar |
| **자동화 적합** | ★★★★☆ |
| **관련** | [`TEST_GUIDE.md`](./TEST_GUIDE.md) 2.2 |

---

### TC-AUTH-06. 이메일 로그인 실패 (미가입 계정)
### pytest tests/auth/test_email_login_failure.py -v

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **전제** | 한 번도 가입하지 않은 이메일 |
| **절차** | 미가입 이메일로 로그인 시도 |
| **기대결과** | `가입 안된 계정` (환경에 따라 `잘못된 계정입니다`로 보일 수 있음) |
| **자동화 적합** | ★★★☆☆ |
| **비고** | Firebase 이메일 열거 방지 설정에 따라 메시지 변동 가능 |

---

### TC-AUTH-07. 로그아웃
### pytest tests/auth/test_sign_up_logout_withdraw.py::test_sign_up_then_logout -v

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 로그인된 상태 |
| **절차** | 1. 메인 AppBar 로그아웃 아이콘 탭<br>2. 결과 확인 |
| **기대결과** | `로그아웃 되었습니다` 메시지<br>로그인 화면으로 이동 |
| **자동화 적합** | ★★★★★ |

---

### TC-AUTH-08. 회원탈퇴 (이메일) — 취소 / 완료
### pytest tests/auth/test_sign_up_logout_withdraw.py::test_sign_up_then_withdraw -v

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **전제** | 일회성 테스트 계정 (탈퇴 후 재사용 불가) |
| **절차** | **A. 취소**<br>1. AppBar 탈퇴 아이콘 탭<br>2. `정말 탈퇴하시겠습니까?` 다이얼로그에서 취소<br><br>**B. 완료**<br>1. 탈퇴 진입 → `탈퇴하기`<br>2. 이메일 계정은 비밀번호 재인증 입력<br>3. 탈퇴 확정 |
| **기대결과** | A. 메인 화면 유지<br>B. `회원탈퇴 완료` 후 로그인 화면 |
| **자동화 적합** | ★★★☆☆ |
| **비고** | 계정 소모형이라 teardown에서 계정 재생성 필요 |

---

### TC-AUTH-09. 소셜 로그인 (카카오 / 네이버 / 구글)

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **전제** | 각 프로바이더 테스트 계정, 에뮬레이터/실기기 로그인 환경 |
| **절차** | 1. 로그인 화면에서 카카오/네이버/구글 버튼 탭<br>2. 외부 로그인 UI에서 인증 완료<br>3. 앱 복귀 |
| **기대결과** | `{이름}님 환영합니다` 후 메인 진입 |
| **자동화 적합** | ★★☆☆☆ |
| **비고** | WebView·외부 앱 전환으로 flaky. 초기에는 **스모크 1건(구글)** 정도만 권장. 카카오톡 앱 의존 플로우는 실기기 중심. |

---

### TC-WATER-01. 메인 화면 기본 UI

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 로그인 완료 |
| **절차** | 메인 화면 로드 대기 |
| **기대결과** | 원형 진행률(현재 잔 / 목표 잔) 노출<br>`물 한 잔 마셨나요?`, `한 잔 = 250ml 기준` 노출<br>`N일 연속` 텍스트 노출<br>주간/월간 차트 영역 노출 |
| **자동화 적합** | ★★★★★ |

---

### TC-WATER-02. 물 잔 추가 (+) — 기본
### pytest tests/water/test_water_cups.py::test_water_plus_increases_cup -v

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 로그인, 오늘 기록 로드 완료 |
| **절차** | 1. 현재 잔 수 / ml 텍스트 읽기<br>2. `+` 버튼 1회 탭<br>3. UI 갱신 대기 |
| **기대결과** | 잔 수 +1<br>ml = 잔 수 × 250<br>목표 미달이면 `{남은잔}잔 더 마셔요!` 문구 |
| **자동화 적합** | ★★★★★ |
| **비고** | 핵심 비즈니스 플로우. 최우선 자동화 대상 |

---

### TC-WATER-03. 물 잔 감소 (−) — 기본
### pytest tests/water/test_water_cups.py::test_water_minus_decreases_cup -v

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 오늘 잔 수가 1 이상 |
| **절차** | 1. `+`로 1잔 이상 확보<br>2. `−` 버튼 1회 탭 |
| **기대결과** | 잔 수 −1, ml 동기화 |
| **자동화 적합** | ★★★★★ |

---

### TC-WATER-04. 목표 달성 문구 전환

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 기본 목표 8잔 |
| **절차** | `+`를 반복해 목표 잔 수에 도달 |
| **기대결과** | `🎉목표 달성!` 문구로 변경 |
| **자동화 적합** | ★★★★☆ |
| **비고** | 테스트 계정의 오늘 기록을 미리 7잔으로 세팅하면 안정적 |

---

### TC-WATER-05. 물 + 연속 탭 (다회 증가)

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 로그인, 오늘 잔 수 = N |
| **절차** | 1. 현재 잔 수 N 확인<br>2. `+` 버튼을 3회 연속 탭<br>3. 각 탭 후(또는 마지막 후) UI 갱신 대기 |
| **기대결과** | 잔 수 = N + 3<br>ml = (N + 3) × 250<br>목표 미달이면 남은 잔 문구가 맞게 갱신 |
| **자동화 적합** | ★★★★★ |
| **비고** | 연타 시 저장 race가 없는지 확인하는 회귀 케이스 |

---

### TC-WATER-06. 물 − 연속 탭 (다회 감소)

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 오늘 잔 수 ≥ 3 |
| **절차** | 1. `+`로 잔 수를 3 이상 맞춤<br>2. `−` 버튼을 3회 탭 |
| **기대결과** | 잔 수가 정확히 3 감소, ml도 동기화 |
| **자동화 적합** | ★★★★★ |

---

### TC-WATER-07. 0잔에서 − 버튼 (하한)

| 항목 | 내용 |
|------|------|
| **우선순위** | P0 |
| **전제** | 오늘 잔 수 = 0 |
| **절차** | 1. 잔 수가 0인지 확인 (필요 시 `−`로 0까지 감소)<br>2. `−` 버튼 추가 탭 |
| **기대결과** | 잔 수가 0 미만으로 내려가지 않음<br>ml = 0 |
| **자동화 적합** | ★★★★★ |

---

### TC-WATER-08. +/− 왕복 후 최종 값 일치

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **전제** | 로그인, 오늘 잔 수 = N |
| **절차** | 1. `+` 2회 → `−` 1회 → `+` 1회<br>2. 최종 잔 수/ml 확인 |
| **기대결과** | 최종 잔 수 = N + 2<br>ml = (N + 2) × 250 |
| **자동화 적합** | ★★★★☆ |
| **비고** | UI 표시값과 Firestore 저장값이 어긋나지 않는지 검증 |

---

### TC-WATER-09. 목표 직전/직후 +/− 문구 전환

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **전제** | 오늘 잔 수 = 7 (목표 8) |
| **절차** | 1. 문구가 `1잔 더 마셔요!`인지 확인<br>2. `+` 1회 → `🎉목표 달성!` 확인<br>3. `−` 1회 → 다시 `1잔 더 마셔요!` 확인 |
| **기대결과** | 목표 달성/미달 문구가 +/−에 따라 즉시 전환 |
| **자동화 적합** | ★★★★☆ |

---

### TC-BADGE-01. 하루 2L(`day_2L`) 뱃지 획득

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **구현 상태** | 백엔드 저장 O / **뱃지 UI 미연결** (AppBar는 `뱃지 기능 개발중입니다.` 다이얼로그만) |
| **전제** | 당일 목표 미달성 상태, 로그인 |
| **조건** | 오늘 잔 수 ≥ 8 (하루 목표 최초 달성) |
| **절차** | 1. `+`로 8잔 달성<br>2. (UI 연결 후) 뱃지 목록/상세에서 `하루 2L 달성` 확인<br>3. UI 전에는 Firestore `users/{uid}/badges/day_2L`에 `acquired: true` 검증으로 대체 가능 |
| **기대결과** | 뱃지 `day_2L` 획득<br>이름: `하루 2L 달성` / 설명: `하루에 8잔 달성!` |
| **자동화 적합** | ★★☆☆☆ (UI 완성 전) → UI 완성 후 ★★★★☆ |

---

### TC-BADGE-02. 7일 연속(`week_7days`) 뱃지 획득

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **구현 상태** | 백엔드 저장 O / 뱃지 UI 미연결 |
| **전제** | `streakDays`가 6인 계정, 오늘 아직 목표 미달성 |
| **조건** | 목표 달성으로 `streakDays`가 **7**이 되는 순간 |
| **절차** | 1. 사전 데이터로 streak=6 세팅<br>2. 오늘 `+`로 8잔 달성<br>3. 뱃지 `7일 연속 달성` 획득 확인 |
| **기대결과** | `users/{uid}/badges/week_7days` 생성, `acquired: true` |
| **자동화 적합** | ★★☆☆☆ |
| **비고** | 실제 7일 대기 대신 **Firestore 시드 데이터**로 전제 조건을 만드는 것을 권장 |

---

### TC-BADGE-03. 30일 연속(`month_30days`) 뱃지 획득

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **구현 상태** | 백엔드 저장 O / 뱃지 UI 미연결 |
| **전제** | `streakDays` = 29, 오늘 목표 미달성 |
| **조건** | 목표 달성으로 streak = **30** |
| **절차** | TC-BADGE-02와 동일 패턴 (시드 → 오늘 8잔 달성 → 뱃지 확인) |
| **기대결과** | 뱃지 `month_30days` / 이름 `30일 연속 달성` |
| **자동화 적합** | ★★☆☆☆ |

---

### TC-BADGE-04. 6개월 꾸준함의 왕(`king_6months`) 뱃지 획득

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **구현 상태** | 백엔드 저장 O / 뱃지 UI 미연결 |
| **전제** | `streakDays` = 179, 오늘 목표 미달성 |
| **조건** | 목표 달성으로 streak = **180** |
| **절차** | 시드 → 오늘 8잔 달성 → 뱃지 확인 |
| **기대결과** | 뱃지 `king_6months` / 이름 `6개월 꾸준함의 왕` |
| **자동화 적합** | ★★☆☆☆ |

---

### TC-BADGE-05. 목표 미달 시 뱃지 미획득 (네거티브)

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **전제** | 신규 또는 뱃지 없는 계정, 오늘 잔 수 = 0 |
| **절차** | `+`로 7잔까지만 기록 (목표 미달) |
| **기대결과** | `day_2L` 뱃지 없음<br>`goal_achieved` 알림도 없음<br>문구는 여전히 `1잔 더 마셔요!` |
| **자동화 적합** | ★★★☆☆ |

---

### TC-CHART-01. 주간 ↔ 월간 차트 토글

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **전제** | 로그인, 메인 화면 |
| **절차** | 1. `주간 수분 섭취량` 선택 상태 확인<br>2. `월간 평균 섭취량` 탭<br>3. 다시 주간 탭 |
| **기대결과** | 월간 선택 시 `이번 달 평균: ...잔/일` 류 텍스트 노출<br>주간으로 복귀 시 주간 차트 영역 표시 |
| **자동화 적합** | ★★★★☆ |
| **비고** | 차트 픽셀/이미지는 검증하지 말고 **텍스트·세그먼트 상태**만 assert |

---

### TC-NOTI-01. 알림 화면 진입 및 뒤로가기

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **전제** | 로그인 |
| **절차** | 1. AppBar 알림 아이콘 탭<br>2. 알림 화면 타이틀 `알림` 확인<br>3. 뒤로가기 |
| **기대결과** | 알림 화면 진입 → 메인 복귀 |
| **자동화 적합** | ★★★★☆ |

---

### TC-NOTI-02. 알림 읽음 처리

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **전제** | 미읽음 알림이 1건 이상 (환영 알림 또는 목표 달성 알림) |
| **절차** | 1. 알림 화면 진입<br>2. 미읽음 항목 탭<br>3. AppBar 미읽음 뱃지 변화 확인(가능 시) |
| **기대결과** | 해당 알림이 읽음 처리됨 |
| **자동화 적합** | ★★★☆☆ |
| **비고** | 신규 가입 직후 `welcome` 알림을 활용하면 재현이 쉬움 |

---

### TC-NOTI-03. 환영 알림(`welcome`) 생성

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **구현 상태** | O (가입/최초 FCM 토큰 저장 시 생성) |
| **전제** | 신규 이메일 회원가입(또는 최초 로그인) |
| **조건** | 해당 uid에 `type=welcome` 알림이 없을 때 최초 1회 |
| **절차** | 1. 신규 계정으로 가입/로그인<br>2. 메인 진입 후 알림 아이콘 탭<br>3. 목록에서 welcome 알림 확인 |
| **기대결과** | 환영 알림 1건 존재, `isRead: false`<br>AppBar 미읽음 뱃지 ≥ 1 |
| **자동화 적합** | ★★★★☆ |
| **비고** | **앱 내 알림 목록** 검증 권장. OS 시스템 푸시는 flaky하므로 제외 |

---

### TC-NOTI-04. 목표 달성 알림(`goal_achieved`) 생성

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **구현 상태** | O (당일 목표 **최초** 달성 시 생성) |
| **전제** | 로그인, 오늘 아직 목표 미달성 |
| **조건** | 오늘 잔 수 ≥ 8 이 되는 첫 저장 |
| **절차** | 1. (선택) 오늘 잔 수를 7로 시드<br>2. `+`로 8잔 달성<br>3. 알림 화면 진입 |
| **기대결과** | 알림 제목: `오늘 물섭취 목표 달성 💧`<br>메시지: `하루 목표 8잔을 모두 마셨어요!`<br>`type`: `goal_achieved`, 미읽음 |
| **자동화 적합** | ★★★★☆ |

---

### TC-NOTI-05. 목표 달성 알림 중복 생성 방지

| 항목 | 내용 |
|------|------|
| **우선순위** | P1 |
| **전제** | 오늘 이미 8잔 달성해 `goal_achieved` 알림이 1건 있는 상태 |
| **절차** | 1. `−`로 7잔 → 다시 `+`로 8잔<br>2. 알림 목록에서 `goal_achieved` 개수 확인 |
| **기대결과** | 당일 `goal_achieved` 알림이 **추가 생성되지 않음** (최초 1회만) |
| **자동화 적합** | ★★★★☆ |
| **비고** | `goalAchievedDate == 오늘`이면 재생성하지 않는 현재 로직 검증 |


---

### TC-DRINK-01. 뱃지 획득 시 음료 잠금 1개 해제

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **구현 상태** | ❌ **현재 UI/로직 미구현** (기능 완성 후 자동화) |
| **전제** | 잠긴 음료가 1개 이상인 계정, 미보유 뱃지 조건 충족 가능 |
| **조건** | 뱃지 1개 신규 획득 |
| **절차** | 1. 잠금 음료 개수 / 해제된 음료 목록 확인<br>2. 조건 충족으로 뱃지 획득 (예: 하루 8잔 → `day_2L`)<br>3. 음료(또는 음료 선택) 화면에서 잠금 상태 재확인 |
| **기대결과** | 잠금 해제된 음료가 **정확히 1개** 증가<br>이미 해제된 음료는 유지 |
| **자동화 적합** | UI 완성 전 ★☆☆☆☆ / 완성 후 ★★★★☆ |
| **비고** | “뱃지 1개 = 음료 잠금 1개 해제” 규칙을 UI·도메인에 명시한 뒤 locator를 고정할 것 |

---

### TC-DRINK-02. 광고 N회 시청 시 음료 잠금 1개 해제

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **구현 상태** | ❌ **현재 UI/로직 미구현** (기능 완성 후 자동화) |
| **전제** | 잠긴 음료 ≥ 1, 광고 시청 UI 존재, 테스트용 광고(또는 mock) 가능 |
| **조건** | 기획된 광고 시청 횟수(예: N회) 충족 |
| **절차** | 1. 잠금 음료 수 확인<br>2. 광고 시청을 요구 횟수만큼 완료<br>3. 음료 잠금 상태 재확인 |
| **기대결과** | 요구 횟수 달성 시 잠금이 **1개** 해제<br>횟수 미달이면 해제되지 않음 |
| **자동화 적합** | ★☆☆☆☆ (광고 SDK·실광고는 flaky) |
| **비고** | 실광고 대신 **테스트 모드/mock 보상 콜백**을 두는 것이 Appium 안정성에 필수 |

---

### TC-DRINK-03. 광고 횟수 미달 시 잠금 유지 (네거티브)

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **구현 상태** | ❌ 미구현 |
| **전제** | 해제에 필요한 광고 횟수 = N (N ≥ 2) |
| **절차** | 광고를 N−1회만 시청 |
| **기대결과** | 음료 잠금 상태 변화 없음<br>시청 진행도만 N−1/N으로 표시(UI가 있다면) |
| **자동화 적합** | UI·mock 완성 후 ★★★☆☆ |

---

### TC-DRINK-04. 뱃지+광고 경로 각각 1개씩 해제 (누적)

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **구현 상태** | ❌ 미구현 |
| **전제** | 잠긴 음료 ≥ 2 |
| **절차** | 1. 뱃지 1개 획득 → 잠금 1개 해제 확인<br>2. 광고 N회 시청 → 잠금 1개 추가 해제 확인 |
| **기대결과** | 총 **2개** 해제, 서로 다른 음료(또는 슬롯)가 풀림<br>한 경로가 다른 경로 보상을 덮어쓰지 않음 |
| **자동화 적합** | UI 완성 후 ★★★☆☆ |

---

### TC-SHELL-01. 뱃지 다이얼로그 (플레이스홀더)

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **전제** | 로그인 |
| **절차** | AppBar 뱃지 아이콘 탭 |
| **기대결과** | `뱃지 기능 개발중입니다.` 안내 |
| **자동화 적합** | ★★★☆☆ |
| **비고** | 기능 완성 전이라 스모크 정도만 유지 |

---

### TC-ERR-01. 네트워크 오류 메시지
### pytest tests/error/test_network_error.py -v

| 항목 | 내용 |
|------|------|
| **우선순위** | P2 |
| **전제** | 로그인 또는 로그인 시도 가능 상태 |
| **절차** | 비행기 모드 / `adb shell svc wifi disable` 후 로그인 또는 물 저장 시도 |
| **기대결과** | `네트워크 연결 상태가 좋지 않습니다` |
| **자동화 적합** | ★★☆☆☆ |
| **관련** | [`TEST_GUIDE.md`](./TEST_GUIDE.md) 2.3 |

---

## 5. 추천 테스트 스위트 구성

### Smoke (매 PR / 매 빌드) — 약 5~8분

| ID | 케이스 |
|----|--------|
| TC-AUTH-01 | 로그인 화면 UI |
| TC-AUTH-04 | 이메일 로그인 성공 |
| TC-WATER-02 | 물 +1 |
| TC-WATER-03 | 물 −1 |
| TC-WATER-07 | 0잔 하한 |
| TC-CHART-01 | 차트 토글 |
| TC-AUTH-07 | 로그아웃 |

### Regression (일 1회 / 릴리즈 전)

| ID | 케이스 |
|----|--------|
| Smoke 전체 | 위 표 |
| TC-AUTH-02 | 회원가입 성공 |
| TC-AUTH-03 | 회원가입 유효성 |
| TC-AUTH-05 | 잘못된 비밀번호 |
| TC-WATER-04 ~ 09 | 물 +/− 시나리오·목표 문구 |
| TC-NOTI-01 | 알림 진입 |
| TC-NOTI-02 | 알림 읽음 |
| TC-NOTI-03 | welcome 알림 |
| TC-NOTI-04 | goal_achieved 알림 |
| TC-NOTI-05 | 목표 알림 중복 방지 |
| TC-AUTH-08-A | 탈퇴 다이얼로그 취소 |

### Manual / Nightly (자동화 보류 또는 제한적)

| ID | 케이스 |
|----|--------|
| TC-AUTH-09 | 카카오 / 네이버 / 구글 로그인 |
| TC-ERR-01 | 네트워크 오류 |
| TC-AUTH-08-B | 회원탈퇴 완료 (계정 소모) |
| TC-BADGE-01 ~ 05 | 뱃지 획득 (UI 연결 전엔 Firestore 검증 또는 보류) |
| TC-DRINK-01 ~ 04 | 음료 잠금 해제 (기능 구현 후) |

---

## 6. 자동화하지 않는 것이 나은 항목 (현재 기준)

| 기능 | 이유 |
|------|------|
| AI 도우미 화면 | 플레이스홀더, 하단 탭 주석 처리로 UI 진입 경로 없음 |
| 목표 변경 / 설정 | UI 미구현 |
| 온보딩 / 프로필 설정 | 화면 없음 |
| 리마인더 스케줄 | UI 없음 |
| 뱃지 UI 전체 플로우 | 저장 로직은 있으나 AppBar는 플레이스홀더 — **TC-BADGE는 케이스만 정의**, UI 후 자동화 |
| 음료 잠금 / 광고 시청 | **앱에 미구현** — TC-DRINK는 기능 스펙용 사전 정의 |
| FCM 시스템 푸시 | OS 권한·기기 의존, Appium으로 불안정 (앱 내 알림 목록은 자동화 가능) |
| 실광고 SDK E2E | flaky — mock/테스트 모드 권장 |
| 차트 픽셀/이미지 비교 | flaky, 유지보수 비용 큼 |

---

## 7. 시나리오 예시 (Python + Appium 관점)

아래는 **구현 가이드용 의사 코드**입니다. 실제 locator는 Semantics/Key 추가 후 확정하세요.

```python
# smoke: 이메일 로그인 → 물 +1/−1 → 목표 달성 알림 → 로그아웃

def test_smoke_water_and_goal_notification(driver):
    # 1) 로그인
    driver.find_element(...).click()  # 기존 계정으로 로그인
    driver.find_element(...).send_keys("e2e_user@test.com")
    driver.find_element(...).send_keys("ValidPass1!")
    driver.find_element(...).click()  # 로그인 하기

    assert "환영합니다" in driver.page_source
    assert "WaterLog" in driver.page_source

    # 2) 물 + / −
    before = read_cups(driver)
    driver.find_element(...).click()  # +
    assert read_cups(driver) == before + 1
    driver.find_element(...).click()  # −
    assert read_cups(driver) == before

    # 3) 목표 달성 → 알림 (사전 시드로 7잔이면 +1만)
    seed_today_cups(7)  # 테스트 헬퍼 또는 Firestore 시드
    driver.find_element(...).click()  # + → 8잔
    assert "목표 달성" in driver.page_source
    driver.find_element(...).click()  # 알림
    assert "오늘 물섭취 목표 달성" in driver.page_source

    # 4) 로그아웃
    driver.find_element(...).click()
    assert "워터로그" in driver.page_source
```

---

## 8. 자동화 안정성을 위한 앱 측 권장 작업

Appium Locating을 쉽게 하려면, 아래 위젯에 `Semantics(identifier: '...')` 또는 `Key`를 추가하는 것을 권장합니다.

| identifier 예시 | 대상 |
|-----------------|------|
| `btn_kakao_login` | 카카오 로그인 |
| `btn_naver_login` | 네이버 로그인 |
| `btn_google_login` | 구글 로그인 |
| `btn_email_sign_up` | 이메일 회원가입 진입 |
| `btn_email_sign_in` | 기존 계정 로그인 진입 |
| `input_email` / `input_password` | 이메일·비밀번호 입력 |
| `btn_login_submit` | 로그인 하기 |
| `btn_water_plus` / `btn_water_minus` | 물 +/- |
| `text_cups` / `text_ml` | 현재 잔 수 / ml |
| `text_goal_status` | `N잔 더 마셔요!` / `목표 달성!` |
| `btn_logout` / `btn_withdraw` | 로그아웃 / 탈퇴 |
| `btn_notification` | 알림 |
| `list_notification_item` | 알림 목록 아이템 |
| `tab_weekly_chart` / `tab_monthly_chart` | 차트 토글 |
| `btn_badge` / `list_badge_item` | 뱃지 (UI 연결 후) |
| `btn_drink_locked` / `btn_watch_ad` | 음료 잠금·광고 (기능 구현 후) |

—

## 9. 한 줄 결론

**지금 바로 자동화할 것:**  
`이메일 로그인 → 물 +/- 시나리오 → 목표 달성 알림 → 차트 토글 → 로그아웃`

**케이스만 정의해 둔 것 (UI/기능 후):**
- 뱃지 획득 (`day_2L` / `week_7days` / `month_30days` / `king_6months`)
- 음료 잠금 1개 해제 (뱃지 1개 또는 광고 N회)

카카오/네이버/구글·실광고·시스템 푸시는 외부 의존이 커서 **2차 자동화**로 두는 것이 좋습니다.
